# Public Directory

Place static assets here (such as images, videos, icons, etc.) that you want to be served at the root path.

For example, a file placed at `public/images/logo.png` can be referenced in your code as `/images/logo.png`.
